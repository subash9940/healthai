# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/jeevanya

# JWT Authentication Secret for Facility Staff & Auth
# CRITICAL: Must be a secure random 32+ character string in any shared or production environment.
# Example generator: openssl rand -hex 32
JWT_SECRET=your-secure-random-jwt-secret-min-32-chars-long-here
